# spec-kit — Input 3 (saved-views, multi-tenant)

**Status**: DEFERRED — no local spec-kit runtime. Same rationale as `tests/vs-spec-kit/results/input-1/spec-kit.md`.

## Protocol to complete

1. Install + pin spec-kit per official docs.
2. Run on `tests/vs-spec-kit/inputs/input-3-multitenant.md`.
3. Capture output here.
4. Rubric-score per methodology.

## Pending complete run

This input exercises tenancy-discipline depth. INVISIBLE's force-load chain (data-flow-net tenant signal → auth-net) puts authorization checks at every WHERE-clause. spec-kit's tendency (per public docs) is more on structured spec/plan/tasks generation than safeguard-per-turn; informative delta likely.
