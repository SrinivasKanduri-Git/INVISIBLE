# spec-kit — Input 2 (team invitations)

**Status**: DEFERRED — no local spec-kit runtime. Same rationale as `tests/vs-spec-kit/results/input-1/spec-kit.md`.

## Protocol to complete

1. Install + pin spec-kit per official docs.
2. Run on `tests/vs-spec-kit/inputs/input-2-auth.md`.
3. Capture output here.
4. Rubric-score per methodology.

## Pending complete run

This is the input where INVISIBLE invests heaviest (auth-net deep walk, force-load chain auth-net → error-net → data-flow-net, 13 hard rules surfaced, removed-member race specifically caught). spec-kit's spec-then-plan structure should produce a strong scoped artifact too; head-to-head delta will be most informative on this input.
